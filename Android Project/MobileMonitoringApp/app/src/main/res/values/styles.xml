<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
    </style>

    <style name="CustomTitle" parent="TextAppearance.ShowcaseView.Title">
        <item name="android:textColor">@color/register_botton</item>
    </style>

    <style name="CustomShowViewStyle" parent="ShowcaseView.Light">
        <item name="sv_backgroundColor">@color/login_button</item>
        <item name="sv_detailTextAppearance">@color/white</item>
        <item name="sv_showcaseColor">@color/register_botton</item>
        <item name="sv_titleTextAppearance">@style/CustomTitle</item>
    </style>
</resources>
